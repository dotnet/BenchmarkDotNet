language: csharp
os:
  - linux
sudo: required
dist: trusty
osx_image: xcode8.3
mono: latest

addons:
  apt:
    packages: 
    - gettext
    - libcurl4-openssl-dev
    - libicu-dev
    - libssl-dev
    - libunwind8

before_script:
 - chmod +x build.sh
script:
  - ./build.sh --verbosity diagnostic