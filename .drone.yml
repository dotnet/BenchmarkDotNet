#---------------------------------#
#      general configuration      #
#---------------------------------#

# branches to build
trigger:
  branch:
    exclude:
    - gh-pages

#---------------------------------#		
#       build configuration       #		
#---------------------------------#
kind: pipeline
name: arm

platform:
  os: linux
  arch: arm

steps:
- name: build
  image: mcr.microsoft.com/dotnet/sdk:5.0.403-buster-slim-arm32v7
  commands:
  - chmod +x build.sh
  - bash ./build.sh
